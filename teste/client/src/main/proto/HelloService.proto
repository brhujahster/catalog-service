syntax = "proto3";
option java_multiple_files = true;
package org.client.grpc

message HelloRequest {
    string firstname = 1;
    string lastName = 2;
}

message HelloResponse {
    string greeing = 1;
}

service HelloOperation {
    rpc hello(HelloRequest) returns (HelloResponse);
}